
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access only for authenticated users.
    // This covers anonymous authentication as well.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
